This repository contains a **Cache Coherence Protocol** model for MESI and specified using `SMTLib2. SMT solver [CVC5](https://cvc5.github.io/) can be used to check or verify this MESI protocol model is safe for a multicore system that contains two cores. Here is the command to run the specification file:

`./cvc5 mesi.smt2`
